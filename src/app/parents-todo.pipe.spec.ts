import { ParentsTodoPipe } from './parents-todo.pipe';

describe('ParentsTodoPipe', () => {
  it('create an instance', () => {
    const pipe = new ParentsTodoPipe();
    expect(pipe).toBeTruthy();
  });
});
